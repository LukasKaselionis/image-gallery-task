import React from 'react';
import './Header.css';

const Header = () => {
    return <header className="header">
        <h2 className="header__title">react image gallery</h2>
        <h4 className="header__description">The image gallery is divided into two columns using Bootstrap library</h4>
    </header>
}

export default Header;